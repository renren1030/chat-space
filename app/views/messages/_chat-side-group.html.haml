.chat__side--group
  - current_user.groups.each do |group|
    = link_to group_messages_path(group) do
      .chat__side--group--name
        = group.name
      .chat__side--group--messages
        メッセージはまだありません。